#include <stdio.h>

int main() {
    // Correct way to declare and initialize an array
    int mynumbers[] = {29, 49, 59, 79};

    // Accessing and printing the third element (index 2) of the array
    printf("The best number is %d\n", mynumbers[2]);

    return 0;
}
